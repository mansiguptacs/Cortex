package com.echowalk.teamc.embedding

import com.echowalk.shared.Frame

/**
 * Turns a [Frame] into an embedding vector for place recognition (CLIP/MobileCLIP on the NPU).
 *
 * STUB ONLY — fill in (milestone M-C0). See docs/team-c.md.
 */
class ImageEncoder {
    fun encode(frame: Frame): FloatArray {
        // TODO(Team C): preprocess -> run encoder .pte -> L2-normalized embedding.
        return FloatArray(0)
    }
}
