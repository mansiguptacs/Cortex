package com.echowalk.teamc

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity

/**
 * Isolated test bench for Team C (Familiar Places). Enroll landmarks from the camera (or a saved
 * video), then localize / guide — no dependency on Teams A/B.
 *
 * TEMPLATE: fill in. See docs/team-c.md.
 * Launch: adb shell am start -n com.echowalk/.teamc.TeamCHarnessActivity
 */
class TeamCHarnessActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // TODO(Team C): set up enrollment + localization UI here.
    }
}
