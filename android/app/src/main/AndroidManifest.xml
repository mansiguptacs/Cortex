<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Permissions are merged in from the modules that own them:
        CAMERA + VIBRATE  -> :shared
        RECORD_AUDIO      -> :teamc
      Per-team harness activities are declared in each team module's manifest.
    -->

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.EchoWalk">

        <!--
          Non-NDK vendor libraries the Qualcomm QNN HTP delegate dlopens at runtime.
          On Android 12+ apps must whitelist these via <uses-native-library>; without
          them the QnnHtpV79Stub.so load fails and the delegate falls back to CPU.
        -->
        <uses-native-library android:name="libcdsprpc.so" android:required="false" />
        <uses-native-library android:name="libadsprpc.so" android:required="false" />
        <uses-native-library android:name="libsdsprpc.so" android:required="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
